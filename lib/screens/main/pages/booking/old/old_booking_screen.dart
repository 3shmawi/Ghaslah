import 'package:flutter/material.dart';

class OldBooking extends StatelessWidget {
  const OldBooking({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
